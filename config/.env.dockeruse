# Docker / 部署环境变量（提交仓库，由 compose env_file 注入容器）
# 业务默认值见 application.yml.dockeruse
# 本地开发：复制 config/.env.example → config/.env；部署环境由 compose 注入本文件

SHELLDER_PROFILE=default

DATABASE_URL=mysql://iot5:password@192.168.109.211:3306/agent_platform
REDIS_HOST=10.30.20.220
REDIS_PORT=7002
REDIS_PASSWORD=

JWT_SECRET=change-me-to-a-long-random-string-in-production
JWT_EXPIRES_IN=7d
CONNECTOR_SECRET_KEY=change-me-to-a-long-random-connector-secret-key
WORKER_INTERNAL_TOKEN=
