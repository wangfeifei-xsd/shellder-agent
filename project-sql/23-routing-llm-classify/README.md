# 23 — 路由 LLM 分类 Prompt

Phase 3 可选 Stage1 LLM 能力分类；默认关闭，需 `tenant.config.routing.llmClassifyEnabled=true` 或环境变量 `ROUTING_LLM_CLASSIFY_ENABLED=true`。

- Prompt key：`routing.classify.system`
- 同步登记：`src/prompt/prompt-keys.ts`
