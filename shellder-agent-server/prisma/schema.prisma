// shellder-agent — Prisma 基线（阶段 01）
// 业务表从阶段 02（tenant）起按模块递增，见 project-sql/

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// 阶段 01 占位：无业务模型。迁移流水线已就绪，_prisma_migrations 由 Prisma 管理。
